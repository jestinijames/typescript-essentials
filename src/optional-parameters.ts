// ****** OPTIONAL PARAMETERS ******

// It means it is not returing anything
function printNameAndAge(name: string, age?: number): void {
  console.log(name);
}

const person = printNameAndAge("jestin", 38);
